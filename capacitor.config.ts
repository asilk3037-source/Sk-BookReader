import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skbookreader.app',
  appName: 'Sk Book Reader',
  webDir: 'dist',
  backgroundColor: '#f5ead8',
  android: {
    backgroundColor: '#f5ead8',
  },
};

export default config;
